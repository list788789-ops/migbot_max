# Задачи проекта migbot_max — бэклог

> Файл для переноса контекста между чатами. Обновляется по ходу работы.
> Последнее обновление: генератор трудового договора (в работе).

---

## В работе сейчас
- **Генератор трудового договора** (ООО «Трестстроймонтаж»). Кнопка «Заключить трудовой
  договор» в карточке → форма (должность, оклад, дата) → docx + запись contract_date.
  Блокировка при пустом tab_number.

## Открытые задачи (ждут очереди)
1. **Должность и оклад — вынести в поля модели** (сейчас вводятся в форме генерации договора
   как переменные с дефолтами «Монтажник»/«30000», значения НЕ проверяются). Когда понадобится
   хранить их per-employee, а не вводить каждый раз — завести поля position/salary в Employee.
2. **OCR паспорта** при вводе нового сотрудника (загрузка скана → распознавание МRZ →
   автозаполнение серии/номера/ФИО/даты рождения). Отложено, отдельная подсистема.
3. **Неявка на медкомиссию — полноценная история** через статус ExamStatus.CANCELLED
   (сейчас временно: «Не пройдено» УДАЛЯЕТ Referral, каскадом сносит Invoice). Вернуться,
   добавить CANCELLED, сохранять историю попыток и счёт. Синхронизировать с bot.py
   (_handle_medical_exam_result — там сейчас старое поведение, расходится с webforms).
4. **Медосмотр «не пройден по болезни»** — отдельный исход (годен/не годен по здоровью),
   пока не разбирали. Обязательство при этом закрыто (процедура пройдена), в отличие от неявки.
5. **Удалить 5 старых роутов полей** карточки (entry_date, entry_country, address,
   contract_date, dactyloscopy_date) — оставлены рабочими рядом с /save, удалить после
   проверки, что /save работает.
6. **working_days_add без учёта праздников РФ** — риск для 1-дневных дедлайнов (ЕФС-1, переезд)
   рядом с праздниками. Подключить производственный календарь (workalendar).
7. **Удалить SystemFlag + эндпоинт /admin/recompute-dactyloscopy** после разового прогона
   бэкфилла дактилоскопии.
8. **9 сотрудников без табельного** (нет в июльском табеле): Алдабергенов, Ахметкали,
   Нарзиев, Сарсенбаев, Собянин, Торебек Сергазы (нет в табеле); Алтеев, Кете Юнус,
   Кете Юсуф (не сматчились — возможно, в табеле под другим написанием). Договор им
   недоступен до присвоения табельного.

## Известные риски / долги
- **Заглушки-даты въезда** у не прибывших — кадровик обнуляет вручную (не прибывшие = пустой
  entry_date, тогда дедлайны не идут; заглушка даёт фантомные просрочки).
- **Двойные пробелы в ФИО** — почищены разово (regexp_replace \\s+ → ' '). Следить, чтобы
  форма ввода не плодила новые.

## Сделано (для контекста)
- Дактилоскопия: тип, поле dactyloscopy_date, правило (entry_date+30 кал), гейт, кнопка-прогон.
- Per-type пороги дедлайнов (lead_days), born-amber для рабочих дней. MSK вместо UTC на дашборде.
- address_since: 30 кал → 1 рабочий (вахта), Belarus 90→7 рабочих.
- SITE_ADDRESS (адрес площадки) в п.3 направления; всем проставлен в БД; согласие оставлено
  на employee.address.
- Реквизиты клиники/заказчика в document_templates (CLINIC_*, PAYER_*, CLINIC_SHORT_NAME).
- Единая кнопка «Сохранить» в карточке + /save с серверным дифом + JS-confirm по опасным полям.
- Форма создания сотрудника /employees/new (категория из гражданства, DRAFT, SITE_ADDRESS).
- Список сотрудников: секции «Активные» / «Ожидают прибытия» (по entry_date).
- Рестайл: акцент #4a90e2, линии карты входа темнее, nav flex-wrap, активный пункт —
  подчёркивание. Крест Белокаменки не трогали (#2f80ed).
- Табельные номера: поле tab_number, импорт из табеля по ФИО (58/67).

## Ключевые факты (не терять)
- Работодатель по трудовому договору = ООО «Трестстроймонтаж». Заказчик медуслуги = ИП Буц С.Ю.
  Это РАЗНЫЕ юрлица в разных документах — не путать.
- Реквизиты ТСМ приняты «как факт без проверки» (по образцу договора Уристемова).
- Все работники живут по одному адресу (SITE_ADDRESS). Переезд по РФ = 7 раб. дней (1 на вахте).
- Порядок деплоя: models.py → deadlines.py → obligations.py → document_templates.py → webforms.py
  (зависимость всегда первой, иначе ImportError на старте).
- Работа только с телефона, файлы целиком (не патчи), правки — с предварительным согласием.
